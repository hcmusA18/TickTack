import React from 'react'
import { View, Text, StyleSheet } from 'react-native'
import { colors } from 'theme'

const InterestGroup = (props) => {
  return (
    <View>
      <Text>InterestGroup</Text>
    </View>
  )
}

const styles = StyleSheet.create({})

export default InterestGroup
